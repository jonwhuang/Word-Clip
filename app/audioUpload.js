// app.controller('uploadCtrl',['$scope', 'Upload', '$http','uploadService',  function($scope, Upload, $http, uploadService){

//   $scope.submit = function() {
//     if ($scope.form.file.$valid && $scope.file) {
//       $scope.upload($scope.file);
//     }
//   };

//   $scope.upload = function(file) {
//     uploadService.audioFile = file[0];
//   }

// }]);